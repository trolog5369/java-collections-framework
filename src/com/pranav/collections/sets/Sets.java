package com.pranav.collections.sets;

public class Sets {
    public static void main(String[] args) {
        // A collection that has no duplicate elements

        // Think of it as a bag which contents we can't see(Which we don't know what's inside)

        // Sets dosen't gurantee order.

        // More formally,sets contain no pair of elements
        // e1 and e2 such that e1 equals e2(e1!=e2)
        // and at most one null element

        // As implied by its name, this interface models 
        // the mathematical set of abstraction
    }
}
