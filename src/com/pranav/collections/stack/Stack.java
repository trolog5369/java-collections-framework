package com.pranav.collections.stack;

public class Stack {
    public static void main(String[] args) {
        // Stack class reprsents a Last-In-First-Out(LIFO) stack of objects.
        // It extends class Vector with five operations that allows a vector to be treated as a stack
        // as well as a method to peek at the top item on the stack,
        // a method to test for whether the stack is empty,and a method
        // to search the stack for an item and discover how far it is from the top

        // A packet of Pringles ie the Last one that goes in is the first one that comes out!
    
        // methods
        // -Push(allows push the element into the stack)
        // -Pop(removes the element at the top of the stack)
        // -Peek(allows to see what's at the top of the Stack without removing it)
    }
}
